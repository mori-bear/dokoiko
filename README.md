# Dokoiko

> MORI-LAB 最優先プロジェクト

## 概要

（プロジェクト説明をここに記載）

## ステータス

- [ ] 要件定義
- [ ] 技術スタック確定
- [ ] MVP開発

## アーキテクチャ方針

### 現在: 単一Webアプリ構成

```
src/
├── components/   # UIコンポーネント
├── pages/        # ページ定義
├── lib/
│   └── api/      # APIアクセス層（← 将来ここを差し替える）
└── types/        # 型定義
```

### 将来: API分離設計

`src/lib/api/` を独立サーバーに切り出す想定で設計する。
分離が必要になった時点で `server/` ディレクトリを活性化する。

```
server/           # 将来のAPIサーバー分離先（現在は空）
```

## セットアップ

```bash
# 依存関係インストール（技術スタック確定後に記載）
```

## 環境変数

`.env.example` を参照してください。
